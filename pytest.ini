[pytest]
# rootdir = thư mục chứa file này. Thêm nó vào sys.path để `import
# crawler_client`, `import helpers`, `import app`... trong tests/
# luôn tìm thấy được, bất kể pytest được gọi từ thư mục nào.
pythonpath = .
testpaths = tests
